# Fingerprints (commit:path:rule:line) of findings that have been read and are
# not credentials. Nothing else belongs here: the scan is expected to be clean,
# so a new finding is a real one rather than noise a reviewer scrolls past.
#
# Every entry below was checked against the blob at that commit, not just the
# current file.

# A placeholder that spells out what it is: "wJalrXUtnFEMI-THE-SECRET-HALF-K7MDENG".
b5f61a2ec065d532e86be4d016e1e7d68a42767b:daemon/internal/server/identity_test.go:generic-api-key:32

# The example vault token printed in the README walkthrough ("tk_9f2c4a7e1b03"),
# short enough to be unusable and never issued by any daemon.
193edc80e3b741248ba41cd3a2cd12b122072658:README.md:generic-api-key:269

# The classifier's own test fixture: a synthetic "sk-..." string whose whole
# purpose is to be recognised as OpenAI-key-shaped. Two commits touched the line,
# so the same fixture has two fingerprints.
582627a3d636184221b09f034c64120ad73112be:daemon/internal/classifier/classifier_test.go:generic-api-key:70
8722a7d6953e1d773f98214fd8bef5d5cb541a0a:daemon/internal/classifier/classifier_test.go:generic-api-key:70

# Two AWS ids that differ by one character. The discovery tests need two
# DISTINCT identities to prove a partial credential does not take a usable
# one's name, so one of the pair cannot be AWS's own published example key —
# and gitleaks allowlists that key by value while having no way to know a
# variant of it was invented too. The working tree no longer contains the
# literal (it is assembled in discover_test.go, so future commits touching
# that file will not re-trip this), but these commits are already published
# and cannot be rewritten again.
7ac544cfb50f047152d74793cd1b997dc53a2819:daemon/internal/template/discover_test.go:aws-access-token:416
5875fcf17f9f7c4d6dde4630c28d044862b829bd:daemon/internal/template/discover_test.go:aws-access-token:374
5875fcf17f9f7c4d6dde4630c28d044862b829bd:daemon/internal/template/discover_test.go:aws-access-token:408
5875fcf17f9f7c4d6dde4630c28d044862b829bd:daemon/internal/template/discover_test.go:aws-access-token:423

# A Stripe-shaped test fixture whose value spells out, in the value itself,
# that it is not a real key. It is NOT quoted here, deliberately -- see the
# entry at the bottom of this file. Same treatment as the AWS pair: assembled
# from parts in the working tree now, pinned here for the commit that
# published it.
afa7aa2b4655d856782916192747da09ec379d19:daemon/internal/server/agent_surface_test.go:stripe-access-token:226

# This file's own previous revision.
#
# The comment above used to quote the fixture verbatim, to be helpful about
# what was being ignored. The rule matches the shape wherever it occurs --
# including inside a comment explaining why that shape is harmless -- so the
# ignore-file became a finding in its own right, and the scan stayed red for
# one more round with a single leak that was entirely self-inflicted.
#
# The quote is gone from the working tree. This pin exists because 382bdd5 is
# already published and public main does not get rewritten for a comment.
#
# The rule for anyone adding an entry here: describe the fixture, never
# reproduce it.
382bdd51e87d7da905e47bfb5f84f94caa5b4184:.gitleaksignore:stripe-access-token:35
